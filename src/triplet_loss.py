# This script will be used to retrain the last layer of the pre-trained model
# for converting the model into to do triplet loss using the encodings from
# the pre-trained model

import tensorflow as tf

